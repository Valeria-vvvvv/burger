# Бургер Чеддер - React версия

Это React версия сайта бургерной "Бургер Чеддер", переделанная с оригинального HTML/CSS/JS проекта.

## Функциональность

### ✅ Реализованные возможности:

- **Адаптивная верстка** - сайт корректно отображается на всех устройствах
- **Навигация** - плавная прокрутка по секциям сайта
- **Каталог бургеров** - отображение всех 12 бургеров с изображениями и описаниями
- **Поиск** - поиск бургеров по названию в реальном времени
- **Смена валют** - переключение между $ → ₽ → BYN → € → ¥ с пересчетом цен
- **Модальное окно регистрации** - форма с валидацией телефона
- **Форма заказа** - с валидацией полей и подсветкой ошибок
- **Уведомления** - красивые всплывающие уведомления
- **Современная архитектура** - компонентный подход, хуки, контекст

### 🎨 Дизайн и UX:

- Полностью сохранен оригинальный дизайн
- Плавные анимации и переходы
- Hover эффекты на карточках и кнопках
- Адаптивная сетка для каталога товаров
- Красивые формы с фокус-состояниями

### 🏗️ Архитектура:

- **React 18** с функциональными компонентами
- **Context API** для управления валютами
- **Модульная структура** - каждый компонент в отдельной папке
- **CSS Modules подход** - изолированные стили
- **Переиспользуемые компоненты** - ProductCard, Modal, Notification

## Структура проекта

```
src/
├── components/
│   ├── Header/          # Шапка сайта с навигацией
│   ├── Hero/            # Главная секция с героем
│   ├── WhyUs/           # Секция "Почему нас выбирают"
│   ├── Products/        # Каталог бургеров
│   │   ├── Products.js
│   │   ├── ProductCard.js
│   │   └── *.css
│   ├── Order/           # Форма заказа
│   ├── Modal/           # Модальное окно регистрации
│   ├── Footer/          # Подвал сайта
│   └── Notification/    # Компонент уведомлений
├── context/
│   └── CurrencyContext.js  # Контекст для управления валютами
├── data/
│   └── burgers.js       # Данные о бургерах
├── assets/              # Изображения и шрифты
└── App.js               # Главный компонент

public/assets/           # Публичные ассеты
├── img/                 # Все изображения
└── fonts/               # Шрифты
```

## Запуск проекта

### Установка зависимостей:

```bash
npm install
```

### Запуск в режиме разработки:

```bash
npm start
```

Откроется браузер на http://localhost:3000

### Сборка для продакшена:

```bash
npm run build
```

## Основные улучшения по сравнению с оригиналом

### 🚀 Производительность:

- Виртуальный DOM для быстрых обновлений
- Компонентная архитектура для переиспользования
- Оптимизированные ре-рендеры

### 🛠️ Разработка:

- TypeScript-ready структура
- Легко расширяемая архитектура
- Четкое разделение логики и представления
- Переиспользуемые компоненты

### 🎯 UX улучшения:

- Более плавные анимации
- Лучшая обработка ошибок
- Закрытие модалки по Escape и клику вне области
- Блокировка скролла при открытой модалке
- Автоматическое скрытие уведомлений

### 📱 Адаптивность:

- Улучшенная мобильная версия
- Гибкая сетка для каталога
- Оптимизированные размеры для планшетов

## Технические детали

### Управление состоянием:

- **CurrencyContext** - глобальное состояние для валют
- **useState** - локальное состояние компонентов
- **useEffect** - побочные эффекты (закрытие модалки по Escape)

### Валидация форм:

- Валидация телефона по регулярному выражению
- Подсветка ошибочных полей
- Очистка ошибок при вводе

### Поиск:

- Фильтрация в реальном времени
- Поиск по названию (регистронезависимый)
- Отображение сообщения при отсутствии результатов

Проект готов к дальнейшему развитию и легко масштабируется!

## 🗂️ Оптимизация проекта

### ✅ Удалены дублирующиеся файлы:

- Оригинальная папка `сайт1/` с HTML/CSS/JS версией
- Дублирующиеся ассеты из `public/assets/`

### 🔧 Оптимизированы импорты:

- **Все изображения** импортируются как ES6 модули
- **Централизованный файл** `imageImports.js` для изображений бургеров
- **Webpack оптимизация** - только используемые изображения попадают в бандл
- **Правильные относительные пути** в CSS файлах

### 🚀 Преимущества оптимизации:

- **Меньший размер бандла** - неиспользуемые изображения исключаются
- **Лучшая производительность** - изображения кэшируются браузером
- **Tree shaking** - только нужные ассеты включаются в сборку
- **Типобезопасность** - ошибки в путях к изображениям видны на этапе сборки

Проект полностью оптимизирован и готов к продакшену! 🎉

## 📄 Структура JSX файлов

### ✅ Все компоненты переименованы в .jsx:

- **App.jsx** - главный компонент приложения
- **CurrencyContext.jsx** - контекст для управления валютами
- **Все компоненты** в папке `components/` имеют расширение `.jsx`

### 🎯 Преимущества JSX расширения:

- **Лучшая читаемость** - сразу понятно что это React компонент
- **IDE поддержка** - лучший синтаксис хайлайтинг и автодополнение
- **Стандарты сообщества** - общепринятая практика в React проектах
- **Линтинг** - ESLint правила специально для JSX

### 📂 Структура компонентов:

```
src/components/
├── Header/
│   ├── Header.jsx    # React компонент
│   └── Header.css    # Стили компонента
├── Products/
│   ├── Products.jsx      # Основной компонент
│   ├── ProductCard.jsx   # Дочерний компонент
│   ├── Products.css      # Стили Products
│   └── ProductCard.css   # Стили ProductCard
└── ... (остальные компоненты)
```

Теперь проект полностью соответствует современным стандартам React разработки! 🚀

## 🔧 Замена валют на админку

### ✅ Удалена функциональность валют:

- **CurrencyContext.jsx** - полностью удален
- **Смена валют** - убрана из Header
- **Фиксированная валюта** - все цены теперь в долларах ($)

### 👤 Добавлен режим администратора:

- **Кнопка админки** в Header (⚙️/👤)
- **Переключение режимов** - обычный/админский
- **Кнопки редактирования** на карточках товаров в админ-режиме
- **Визуальные индикаторы** активного режима

### 🎯 Функциональность админки:

```jsx
// Состояние админки в App.jsx
const [isAdminMode, setIsAdminMode] = useState(false);

// Передача в компоненты
<Header isAdminMode={isAdminMode} onToggleAdmin={...} />
<Products isAdminMode={isAdminMode} />
<ProductCard isAdminMode={isAdminMode} />
```

### 🎨 Стили админки:

- **Неактивная кнопка**: серая (⚙️)
- **Активная кнопка**: оранжевая (👤)
- **Кнопки редактирования**: появляются только в админ-режиме

Готово к дальнейшему развитию админ-панели! 🚀

## 🧭 Роутинг и навигация

### ✅ Добавлен React Router:

- **react-router-dom** - для клиентского роутинга
- **BrowserRouter** - обертка в App.jsx
- **Routes/Route** - конфигурация маршрутов

### 📍 Доступные страницы:

```
/ - Главная страница (Hero + WhyUs + Products + Order)
/products - Каталог бургеров
/profile - Личный кабинет пользователя
/admin - Панель администратора
```

### 🏗️ Структура страниц:

```
src/pages/
├── HomePage.jsx - полная главная страница
├── ProductsPage.jsx - каталог с описанием
├── AdminPage.jsx - управление продуктами + статистика
├── ProfilePage.jsx - профиль + история заказов
└── *.css - стили для страниц
```

### 🎯 Навигация в Header:

- **Логотип** → главная страница
- **Главная** → `/`
- **Меню бургеров** → `/products`
- **Профиль** → `/profile`
- **Кнопка "Войти"** → `/admin`

### 🔧 Программная навигация:

```jsx
import { useNavigate } from "react-router-dom";
const navigate = useNavigate();
navigate("/admin"); // переход на админку
```



 // Import the functions you need from the SDKs you need
import { initializeApp } from "firebase/app";
import { getAnalytics } from "firebase/analytics";
// TODO: Add SDKs for Firebase products that you want to use
// https://firebase.google.com/docs/web/setup#available-libraries

// Your web app's Firebase configuration
// For Firebase JS SDK v7.20.0 and later, measurementId is optional
const firebaseConfig = {
  apiKey: "AIzaSyCS2BzkfMC8Cu6i40NY41vu_R0PFUUFJpw",
  authDomain: "application-7735c.firebaseapp.com",
  projectId: "application-7735c",
  storageBucket: "application-7735c.firebasestorage.app",
  messagingSenderId: "34605007025",
  appId: "1:34605007025:web:04f81bda5c66e6f16a6290",
  measurementId: "G-BGKZXHENCK"
};

// Initialize Firebase
const app = initializeApp(firebaseConfig);
const analytics = getAnalytics(app);